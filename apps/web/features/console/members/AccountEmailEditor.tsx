"use client";
import { useEffect,useState } from "react";
export function AccountEmailEditor({email,busy,canEdit,onSave}:{email:string;busy:boolean;canEdit:boolean;onSave:(email:string)=>Promise<void>}){const [value,setValue]=useState(email);useEffect(()=>setValue(email),[email]);return <div className="border border-slate-200 p-4"><p className="text-xs font-semibold text-slate-500">账号邮箱（可选）</p><div className="mt-2 flex gap-2"><input className="min-w-0 flex-1 border border-slate-300 px-3 py-2 text-sm" disabled={!canEdit||busy} placeholder="未设置" type="email" value={value} onChange={(event)=>setValue(event.target.value)}/>{canEdit?<button className="bg-blue-950 px-3 text-xs font-bold text-white disabled:bg-slate-400" disabled={busy||value===email} onClick={()=>void onSave(value)}>保存</button>:null}</div><p className="mt-2 text-xs text-slate-500">用于安全通知；留空会保存为 NULL，且不会在官网公开。</p></div>}
